# Claude Code local settings (may contain approved shell commands with secrets)
.claude/settings.local.json
.claude/

# Generated by build script — not needed in git (Netlify regenerates on deploy)
catalog.json
